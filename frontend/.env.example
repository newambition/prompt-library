# Frontend Environment Variables
# Copy this file to .env.local (or .env.development, .env.production)
# and fill in your actual values.
# Files like .env, .env.local, etc., are gitignored and should NOT be committed.
# This .env.example file IS SAFE TO COMMIT.

# Auth0 Configuration (obtain from your Auth0 Application settings)
REACT_APP_AUTH0_DOMAIN=your-auth0-domain.auth0.com
REACT_APP_AUTH0_CLIENT_ID=your-auth0-client-id
REACT_APP_AUTH0_AUDIENCE=your-auth0-api-audience

# API Base URL (where your backend is running)
# For development, this is typically http://localhost:8000
# For production, this will be your deployed backend URL
REACT_APP_API_URL=http://localhost:8000